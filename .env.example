DATABASE_URL="postgresql://user:password@localhost:5432/gmao?schema=public"

# Auth.js — générer une valeur aléatoire en production (ex: `openssl rand -base64 32`)
AUTH_SECRET="change-me"

# Optionnel — personnalise le compte créé par `npm run db:seed`
# SEED_ADMIN_EMAIL="admin@demo.local"
# SEED_ADMIN_PASSWORD="admin1234"
# SEED_COMPANY_NAME="Mon entreprise"
